<UserControl
	x:Class="AxisCameraMPPlugin.Configuration.View.WizardPageTwo"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:services="clr-namespace:AxisCameraMPPlugin.Mvvm.Services;assembly=AxisCameraMPPlugin.Mvvm"
	services:WindowServiceBehaviors.IsRegisteredView="True">

	<!-- Resources -->
	<UserControl.Resources>
		<ResourceDictionary Source="WizardPageResources.xaml"/>
	</UserControl.Resources>

	<!-- Root element -->
	<Grid>

		<!-- Row and column definitions -->
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition/>
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		
		<!-- Name -->
		<TextBlock Text="Name:"/>
		<TextBox
			x:Name="nameTextBox"
			x:FieldModifier="private"
			Grid.Column="1"
			MaxLength="64"
			Text="{Binding
				Name,
				Mode=TwoWay,
				UpdateSourceTrigger=PropertyChanged,
				ValidatesOnDataErrors=True}"/>
		
		<!-- Snapshot group -->
		<GroupBox
			Grid.Row="1"
			Grid.ColumnSpan="2"
			Header="Snapshot">
			
			<Grid HorizontalAlignment="Center">

				<!-- Snapshot image -->
				<Image
					Source="{Binding Snapshot, Converter={StaticResource ImageBytesConverter}}"/>
				
				<!-- Refresh button -->
				<Button
					VerticalAlignment="Bottom"
					HorizontalAlignment="Right"
					Style="{x:Null}"
					ToolTip="Refresh snapshot"
					Command="{Binding RefreshCommand}">
					<Button.Content>
						<Image
							Height="16"
							Source="/AxisCameraMPPlugin.Configuration;Component/Resources/RefreshArrow_Green_16x16.png"/>
					</Button.Content>
				</Button>
			</Grid>
		</GroupBox>
	</Grid>
</UserControl>
